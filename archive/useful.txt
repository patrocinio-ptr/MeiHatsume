
get server:

server = await client.fetch_guild(id)

get members:

members = server.members

get member:

await client.fetch_member(id)



get channel:

asyn def (interaction, channel : discord.TextChannel)

channel
//
channel = await client.fetch_channel(id)

members in role:

get server()
get members()
for member in members
	if "role" in member.role is not None	
		tem
	else
		não tem



fazer comando com variaveis:
exec(f"{varName} = '{varContents}'")



histórico de msgs:

channel.history = retorna uma lista em array com os objetos das mensagens



comando na dm:
event on_message(message)
	if message.channel == client.DMchannel

ou 
if isinstance(ctx.channel, discord.channel.DMChannel):